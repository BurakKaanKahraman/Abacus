{
  "name": "abacus-e2e",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "End-to-end tests driving the running stack through a real browser",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
